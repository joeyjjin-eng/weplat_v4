# 위플랫 v4.0 디자인 시스템

위플랫(Weplat)은 통신·생활비·보험을 한 앱에서 다루는 **생활 금융 서비스**다. v4.0 앱은 네 개의 탭으로 나뉜다.

- **안심** — 하루 한 번 누르는 안심 체크인. 보호자에게 상태를 전달한다.
- **혜택** — 생활비 절감 계산, 인터넷·TV·모바일 결합 혜택, 요금제 추천.
- **보험** — 미청구(숨은) 보험금 조회, 보험금 청구, 상담 연결.
- **더보기** — 계정, 전체 기능 런처, 상담·설정.

제품 형태는 **Android WebView 하이브리드 앱** 하나다. 웹 자산(HTML/CSS/JS)을 그대로 얹고 네이티브는 얇은 셸(`kr.co.weplat.app`)만 담당한다. 기준 뷰포트는 360×800.

## 출처 (Sources)

| 출처 | 내용 | 비고 |
|---|---|---|
| `weplat_v4.0/` (첨부 코드베이스) | `index.html` 4탭 SPA 시안, `assets/css/tokens.css`(토큰 원본), `shell.css`, `assets/js/app.js`, `android/` 래퍼 | 색·라운드·그림자·타이포 토큰과 컴포넌트 인벤토리의 **1차 출처** |
| `위플랫 에셋.fig` (첨부 Figma) | Page 1 · 7개 프레임 — 마스코트 3종, 벡터화 캐릭터 1종, 로고 락업(심볼+워드마크), 프로필 200×200 2종 | **브랜드 에셋의 1차 출처**. 컴포넌트·변수·텍스트 스타일은 0개 (순수 에셋 파일) |

읽는 사람이 두 출처에 접근하지 못할 수 있으므로, 필요한 값과 에셋은 모두 이 프로젝트 안에 복제해 두었다.

### 소스에 없어서 만들지 않은 것
- Figma 파일에 컴포넌트 세트·Variable 컬렉션·텍스트 스타일이 **하나도 없다** (METADATA 기준 0/0/0). 따라서 컴포넌트 인벤토리는 코드베이스 시안에서 실제로 반복되는 패턴만으로 구성했고, 흔한 프리미티브(Toast, Modal, Input, Tabs 등)는 소스에 없으므로 **만들지 않았다**.
- 입력 폼, 검색, 다크 테마, 온보딩, 로그인 화면은 시안에 존재하지 않는다.

### 의도적 추가 (Intentional additions)
- **Logo / Mascot 컴포넌트** — Figma 에셋을 코드에서 쓰기 위한 얇은 래퍼. 원본 시안의 GNB는 로고 자리에 "W" 글자 박스를 썼는데, Figma에 실제 스마일 심볼이 있으므로 `AppBar`에서 심볼로 교체했다.

---

## CONTENT FUNDAMENTALS — 카피 규칙

**언어는 한국어 존댓말(해요체) 하나.** 영어 UI 문구는 없다.

**사용자를 부르는 법.** 2인칭 대명사("당신")를 쓰지 않는다. 사람 이름에는 항상 님을 붙인다 — `김영희님`. 소유는 "내"로 쓴다 — `내 보험 점검`, `내 보험금 조회하기`. "귀하", "고객님" 없음.

**말투.** 앱이 사용자에게 말을 건다. 서술어는 `-해요 / -드려요 / -조회해요` — 부드럽고 짧다. 명령형("입력하세요")은 쓰지 않고, 조건절로 부담을 먼저 없앤다:
> 챗봇에 지출만 알려주면 / 예상 절감액을 바로 계산해요
> 홈택스만 연결하면 / 못 받은 보험금을 1분 안에 조회해요
> 이용 조건만 넣으면 / 받을 수 있는 혜택을 정리해요

패턴이 고정돼 있다: **"…만 …하면(넣으면/알려주면) → 우리가 …해드려요."** 사용자의 노동은 최소로, 결과는 구체적으로.

**질문형 헤드라인.** 가장 큰 글씨는 질문이다 — `지금 괜찮으신가요?`. 답은 버튼이 대신한다 — `괜찮아요`, `도움이 필요해요`.

**CTA.** 동사+하기 + 화살표 — `계산하기 →`, `추천 받기 →`, `시작하기 →`, `내 보험금 조회하기 →`. 화살표는 반각 `→` 하나, 앞에 공백 한 칸.

**숫자는 근거로만.** 카드마다 정확히 하나의 숫자 태그가 붙는다 — `평균 월 37,000원`, `평균 발견 약 78만원`, `결합 할인 최대 4건`, `3사·알뜰폰 비교`. "평균/최대/약"으로 과장을 눌러 쓴다. 진행도는 `0 / 2` 형태(공백 포함).

**가운뎃점(·)이 이 브랜드의 접속사다.** `인터넷·TV·모바일`, `서류 제출·진행 현황 확인`, `보험·교통사고·통신 담당자 연결`, `알림·설정`. 쉼표보다 조밀하고 슬래시보다 부드럽다.

**길이.** GNB 제목 2~4자, 탭 라벨 2~3자, 퀵그리드 라벨 6자 내외, 카드 제목 4~8자, 설명은 두 줄 고정(`<br>`로 직접 끊어 균형을 맞춘다).

**줄바꿈은 어절 단위로만.** 한글이 어절 중간에서 끊기면("안심 체/크인", "로그인하/고 저장") 바로 티가 난다. `word-break: keep-all`이 `tokens/base.css`에 전역으로 걸려 있고, 짧은 액션 라벨(더보기 링크, 배너 우측 액션, 탭 라벨, 하단 링크)은 아예 `white-space: nowrap`으로 한 줄을 보장한다. 라벨이 길어 넘칠 것 같으면 줄을 늘리지 말고 **글자 수를 줄인다.**

**이모지는 쓰지 않는다.** 아이콘은 전부 Material Symbols. 느낌표도 거의 없다. 톤은 밝지만 들뜨지 않는다 — 돌봄과 돈을 다루는 앱이라 과장은 금물.

---

## VISUAL FOUNDATIONS

### 핵심 비주얼 컨셉 — 그라디언트 + 글라스

이 브랜드의 정체성은 색 하나하나가 아니라 **"파랑→보라 그라디언트 위에 반투명 유리 면을 얹는다"**는 한 문장이다. 화면을 새로 만들 때 이 두 가지가 함께 등장하지 않으면 위플랫처럼 보이지 않는다.

**1) 그라디언트가 바탕이다.** 가장 중요한 면(안심 화면 전체, 혜택 캐러셀의 첫 카드, 모든 주 CTA)은 단색이 아니라 그라디언트로 칠한다. 각도는 두 개로 고정 — 넓은 면 `158deg`, 알약 CTA `100deg`. 색 정지점도 고정이다.

**2) 그 위의 요소는 유리가 된다.** 그라디언트 면 위에서는 카드/버튼/아이콘 타일을 흰색으로 덮지 않고 **반투명 흰 면 + 밝은 흰 테두리**로 처리해 바탕색이 비쳐 보이게 한다. 이것이 위플랫의 글라스 표면이며 값이 정해져 있다:

| 용도 | 채움 | 테두리 | 토큰 |
|---|---|---|---|
| 유리 CTA ("도움이 필요해요") | `rgba(255,255,255,.18)` | `1px solid rgba(255,255,255,.46)` | `--glass-fill` / `--glass-line` |
| 유리 아이콘 타일 (히어로 카드 우상단) | `rgba(255,255,255,.22)` | 없음 | `--glass-tile` |
| 유리 위 텍스트 | `#FFFFFF` (본문) · `.88` (설명) · `.86` (라벨) · `.94` (아이콘) | — | `--on-brand*` |
| 블러 | `blur(14px)` — **선택적 강화**, 아래 주의 참고 | — | `--glass-blur` |

**3) 유리는 그라디언트 위에서만.** 흰 배경·회백색 배경 위에서는 유리를 쓰지 않는다. 거기서는 순백 카드 + 실선 테두리 + 물든 그림자가 대응 언어다. 유리와 흰 카드를 같은 면 위에 섞지 않는다.

**4) 깊이는 블러가 아니라 그림자로 만든다.** 유리 면 자체에는 그림자를 주지 않고, 아래에 깔린 흰 카드/CTA가 파랑으로 물든 그림자를 진다. 유리는 떠 있는 게 아니라 바탕에 얇게 얹혀 있는 느낌이다.

> **주의 (소스와의 차이).** 첨부된 v4.0 시안 코드에는 `backdrop-filter`가 **없다** — 알파 채움과 테두리만으로 유리 느낌을 낸다. `--glass-blur`는 실제 배경이 복잡할 때(사진·마스코트 위) 쓰라고 넣어둔 토큰이며, 기존 화면을 재현할 때는 **끄는 것이 원본에 충실하다.** 실제 앱에서 블러를 쓰고 계시면 알려주세요 — 기본값으로 승격하겠습니다.

### 색
브랜드 축은 **파랑 → 보라** 단 하나다. `#0D76FF`(blue-500)가 앵커, `#3D7BFF` → `#5F6BFF` → `#7C4DFF`로 이어진다. 채도 높은 색은 이 축과 카테고리 틴트(amber/teal) 외에는 없다.

- **그라디언트 각도는 두 개뿐.** 화면·히어로 카드는 `158deg`(위→아래로 흐르는 대각), 알약 CTA는 `100deg`(거의 가로). 다른 각도를 만들지 않는다.
- **잉크는 남색을 머금은 회색.** `#0B1B3A` → `#8A94A6`. 순수 검정·순수 회색은 쓰지 않는다.
- **배경은 차가운 회백색.** 화면 `#F4F7FC`, 앱 프레임 바깥 `#E9EDF4`. 카드만 순백 `#FFFFFF`.
- **틴트는 도메인에 고정.** 통신·인터넷 = amber(`rgba(255,176,32,.20)` / 잉크 `#C98207`), 보험 = violet, 절감 = teal(`#00A98D`), 그 외 = blue. 이 매핑은 임의로 바꾸지 않는다.
- **경고색이 없다.** 빨강·주황 경고 상태가 시안에 존재하지 않는다. 진행 배너도 연파랑(`#EAF1FE`)이다.

### 타이포
**Pretendard Variable** 한 종. 웨이트는 **700과 800 두 단계**만 쓰고(본문 문단만 400), 크기는 12·13·14·15·16·17·19·27·28·31px로 이미 고정돼 있다 — 새 사이즈를 만들지 않는다.

**본문 크기(14~16px)에는 볼드를 쓰지 않는다.** 리스트 행 제목·라벨처럼 본문 크기로 놓이는 글자는 **레귤러(400~500)** 로 간다. 볼드(700)는 그보다 큰 제목(17px 이상)과 버튼·CTA에만 남긴다. 본문 크기에 볼드를 얹으면 리스트 전체가 무거워지고, 진짜 제목과 위계가 겹친다.

자간은 **글씨가 클수록 더 조인다**: 27px 카드 제목 `-.04em`, 28/31px 헤드라인 `-.035em`, 17~19px 제목 `-.025em`, 14~15px 행 제목 `-.02em`, 13px 이하는 0. **본문 행간은 1.4로 고정** — 1.5도, 1.3도 아니다. `--lh-body`를 그대로 쓰고 개별 요소에서 line-height를 다시 지정하지 않는다.

### 레이아웃
좌우 여백의 기본값은 **22px**. 단 **배경이 있는 요소(카드·박스·리스트 컨테이너)는 −2px 한 20px**에 놓는다. 같은 값을 주면 면을 가진 박스가 글자보다 안쪽으로 더 들어가 보이기 때문에, 광학적으로 같은 선에 맞추려면 박스를 2px 더 내보내야 한다. 즉 **글자 22 / 박스 20**이 한 줄로 보이는 상태다.

세로 간격도 같은 이유로 **뒤에 오는 게 박스면 더 띄운다**. 제목·라벨 바로 아래가 카드·박스면 **12px**, 그냥 텍스트면 **8~11px**. 면을 가진 박스는 자기 테두리만큼 시각적으로 위로 붙어 보여서, 텍스트와 같은 간격을 주면 제목에 달라붙는다.

제목이 두 단(큰제목 → 부제 → 내용)일 때는 **간격이 아래로 갈수록 좁아져야** 한다. 큰제목 → 부제 **20px**, 부제 → 내용 **11px**, 그리고 앞 덩어리의 끝 → 다음 부제는 그보다 넓은 **25px**. 부제는 위가 아니라 자기 내용에 붙어 보여야 묶임이 읽힌다 — 이 순서가 뒤집히면 부제가 앞 덩어리에 끌려가고 내용은 떠 보인다.

카드 사이 12px, 카드 내부 블록 사이 14px, 카드 안쪽 24/20px.

화면은 **스크롤하지 않는다**(`overflow:hidden`). 세로로 넘치면 콘텐츠를 줄인다. 가로 스크롤은 두 군데에만 있다 — 필터 칩 줄, 288px 카드 캐러셀(`scroll-snap-type:x mandatory`, 스냅 center). 스크롤바는 항상 숨긴다(`.noscroll`).

고정 요소는 상단 GNB와 하단 탭바(70px + safe-area). 상단은 `max(env(safe-area-inset-top), 24px)`로 확보한다.

### 모서리
액션은 전부 **알약(999px)** — 사각 버튼이 존재하지 않는다. 면은 7 / 11 / 12 / 14 / 15 / 18 / 20 / 22 / 24 / 28px로 **요소 크기에 비례**한다: 22px 로고 마크 7, 32px 아이콘 타일 11, 34~36px 타일 12, 배너 14, 44px 타일 15, 리스트 카드 18, 그리드 카드 20, 유리 CTA 22, 기능 카드 24, 안심 상태 카드 28. 원형은 프로필 이니셜(46px)과 상태 오브(124px)뿐.

### 카드
흰 배경 + `1px solid rgba(11,27,58,.08)` 실선 + 큰 그림자. 히어로 카드만 테두리 없이 그라디언트로 채운다. 컬러 좌측 보더, 점선, 이중 테두리 같은 장치는 쓰지 않는다.

### 그림자
**검정이 아니라 남색·파랑으로 물든 그림자**다. 기본 카드 `0 14px 32px rgba(5,27,80,.10)`, 강조 카드 `0 16px 36px rgba(5,27,80,.12)`, 히어로 `0 18px 40px rgba(13,118,255,.28)`(파랑!), 상태 카드 `0 20px 44px rgba(5,27,80,.22)`, CTA `0 8px 18px rgba(13,118,255,.28)`, 보라 CTA `rgba(124,77,255,.26)`. y 오프셋이 blur의 절반쯤 되는 깊고 부드러운 그림자. **inner shadow는 쓰지 않는다.**

### 투명·블러
투명은 세 가지 용도뿐 — (1) 카테고리 틴트(색 위 12~20% 알파), (2) 그라디언트 위의 유리 표면(위 "핵심 비주얼 컨셉" 참고), (3) 그라디언트 위 보조 텍스트(`.86~.94`). 회백색/흰 배경 위 요소를 알파로 흐리지 않는다 — 거기서는 실색을 쓴다.

백드롭 블러(`--glass-blur: 14px`)는 **옵트인**이다. 원본 시안은 블러 없이 알파만으로 유리를 표현하므로, 기존 화면 재현에는 쓰지 않고 사진·마스코트처럼 복잡한 바탕 위에 유리를 얹을 때만 켠다.

### 상태
- **hover** — 링크는 `#0D76FF → #0A5FCC`로 어두워진다. 모바일 앱이라 hover 설계는 최소.
- **press** — `opacity: .7` (탭바 기준). 크기 변화·색 반전은 쓰지 않는다.
- **active(선택)** — 색 + 아이콘 FILL + 웨이트가 **함께** 바뀐다. 탭바: `--ink-400`/700/FILL 0 → `--blue-500`/800/FILL 1. 칩: 흰 배경+테두리 → 그라디언트 채움.
- **disabled** — 투명도로만 낮춘다(0.45).
- **애니메이션** — 시안에 키프레임 애니메이션이 없다. 상태 변화는 짧은 페이드/컬러 전환(.15~.2s ease) 정도로 절제한다. 바운스·스프링·패럴랙스 없음.

### 이미지·일러스트
사진을 쓰지 않는다. 유일한 이미지는 **흰 곰 마스코트** 벡터 4종으로, 얼굴이 곧 브랜드 스마일 심볼이다(눈 두 개 + 아래로 휜 곡선). 채도가 거의 없는 흰색·미색에 노란 귀, 붉은 옷 정도의 포인트. 배경 패턴·텍스처·노이즈는 없다.

---

## ICONOGRAPHY

- **Material Symbols Rounded**(Google Fonts CDN, `opsz 24, wght 500, GRAD 0, FILL 0..1`) 아이콘 폰트 하나로 통일. 프로젝트 안에 커스텀 아이콘 SVG는 없다.
- 사용법: `<span class="ms">savings</span>`. `.ms` 클래스는 `tokens/base.css`에 있다. 채운 아이콘은 `.ms.is-filled`(`font-variation-settings:'FILL' 1`).
- **FILL 1은 "지금 켜져 있음"에만.** 활성 탭 아이콘, 안심 상태 오브의 `thumb_up`. 그 외는 전부 아웃라인.
- 크기는 놓이는 자리로 고정: GNB 20, 탭바 24, 리스트 앞 19, 카드 뱃지 20~21, 하단 링크 18, chevron 20~22, 상태 오브 64.
- **아이콘은 맨몸으로 놓지 않는다.** 항상 둥근 사각 틴트 타일(`IconTile`) 안에 넣는다. 예외는 GNB 아이콘, chevron, 하단 링크 아이콘.
- 실제로 쓰이는 글리프: `favorite savings shield grid_view router sim_card smartphone search_insights receipt_long chat_bubble person settings notifications account_circle emergency_home thumb_up directions_car arrow_forward chevron_right`.
- **이모지·유니코드 아이콘은 쓰지 않는다.** 단, 화살표 `→`와 가운뎃점 `·`은 텍스트 글리프로 쓴다.
- 브랜드 마크(스마일 심볼)는 아이콘 폰트가 아니라 `assets/symbol.svg`.

---

## 폰트 파일에 대한 참고

두 폰트 모두 **원본 앱과 동일하게 CDN에서 로드**한다(`tokens/fonts.css`). 폰트 바이너리를 프로젝트에 번들하지 않았다 — 오프라인/사내망 배포가 필요하면 Pretendard Variable(woff2)과 Material Symbols Rounded 파일을 받아 `assets/fonts/`에 넣고 `@font-face`로 바꿔야 한다. 대체 폰트를 쓴 곳은 없다.

---

## 파일 안내 (Index)

```
styles.css                  ← 소비 프로젝트가 링크하는 단 하나의 진입점 (@import만)
tokens/                     fonts · colors · typography · radius · elevation · layout · base
assets/                     logo-lockup.svg · symbol.svg
                            mascot-1/2/3.svg · illustration-image31.svg
                            profile-tile-light.svg · profile-tile-dark.svg
components/brand/           Logo · Mascot
guidelines/                 파운데이션 스펙 카드 16장 (Colors · Type · Spacing · Brand)
SKILL.md                    Claude Code 등에서 스킬로 쓸 때의 진입점
```

### 컴포넌트 목록

- **brand** — `Logo`, `Mascot`

**컴포넌트 2개(브랜드 에셋 래퍼) / 파운데이션 카드 20장.** 이 시스템은 UI 컴포넌트 라이브러리가 아니라 **토큰 · 타이포 · 브랜드 에셋 · 규칙 문서**를 제공한다. 화면은 이 규칙을 읽고 직접 만든다.
